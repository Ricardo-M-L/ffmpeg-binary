# Windows 打包环境 - 轻量版
# 使用 NSIS (原生 Linux 工具,无需 Wine)

FROM golang:1.24-bullseye

# 安装 NSIS (原生支持 Linux/ARM64)
RUN apt-get update && \
    apt-get install -y nsis nsis-common && \
    rm -rf /var/lib/apt/lists/*

# 设置工作目录
WORKDIR /workspace

CMD ["/bin/bash"]