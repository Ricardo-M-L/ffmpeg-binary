# 📦 打包示例

这是一个完整的打包流程示例,演示如何为不同平台打包 GoalfyMediaConverter 服务。

## 🎨 步骤 1: 生成应用图标

图标已经设计好了(`assets/icons/icon.svg`),现在需要将 SVG 转换为各平台格式:

```bash
# 生成所有平台的图标
./scripts/generate-icons.sh
```

**输出:**
- `assets/icons/icon.icns` - macOS
- `assets/icons/icon.ico` - Windows
- `assets/icons/icon.png` - Linux (512x512)

> **注意**: 需要安装 ImageMagick: `brew install imagemagick librsvg`

---

## 🍎 步骤 2: 打包 macOS PKG (生产就绪)

```bash
# 执行打包
./scripts/build-macos-pkg.sh
```

**输出:**
```
╔══════════════════════════════════════════════════════════════╗
║           macOS PKG 安装包构建工具                         ║
╚══════════════════════════════════════════════════════════════╝

==> 清理旧文件...
==> 编译 macOS Universal Binary...
    架构: amd64 + arm64
    ✅ Universal Binary 已生成
==> 创建 .app 包...
    ✅ .app 包已创建
==> 创建安装脚本...
    ✅ 安装脚本已创建
==> 创建安装界面文本...
    ✅ 安装界面文本已创建
==> 构建组件包...
    ✅ 组件包已创建
==> 构建最终安装包...
    ✅ 安装包已创建
==> 设置安装包图标...
    ✅ 图标已设置

╔══════════════════════════════════════════════════════════════╗
║                 ✅ 打包完成!                                ║
╚══════════════════════════════════════════════════════════════╝

📦 安装包: dist/macos/GoalfyMediaConverter-Installer.pkg
```

**用户安装体验 (像爱奇艺、网易云那样):**

1. 用户双击 `GoalfyMediaConverter-Installer.pkg`
2. 打开标准的 macOS 安装器界面,显示:
   - **欢迎页面**: 介绍产品功能和系统要求
   - **安装位置**: /Applications/ (系统默认)
   - **安装进度**: 显示安装进度条
   - **完成页面**: 显示安装成功信息
3. 点击"继续" → "安装" → 输入管理员密码 → 完成
4. **安装过程自动完成以下操作:**
   - ✅ 安装应用到 /Applications/
   - ✅ 自动下载并安装 FFmpeg 静态二进制 (带超时和重试)
   - ✅ 无需 Homebrew,适配所有 Mac 用户
   - ✅ 支持 Apple Silicon (arm64) 和 Intel (amd64)
   - ✅ 配置 launchd 自启动服务
   - ✅ 立即启动后台服务
5. **服务自动在后台运行,没有任何终端窗口**
6. 服务地址: http://127.0.0.1:28888
7. **卸载**: 直接将应用拖到废纸篓,自动清理所有文件和服务!

**生产级特性:**
- ✅ 真正的图形化安装界面 (macOS 原生 PKG)
- ✅ 符合 macOS 标准安装体验
- ✅ 自动安装 FFmpeg (不依赖 Homebrew,带超时防卡死)
- ✅ 无需双击 .command 文件
- ✅ 无终端黑窗口
- ✅ 后台静默运行
- ✅ 自动配置开机自启动
- ✅ 固定端口 28888
- ✅ 支持 Universal Binary (Intel + Apple Silicon)
- ✅ 拖到废纸篓自动卸载 (1分钟内自动清理)
- ✅ 完整的安装日志 (`~/Library/Logs/goalfy-mediaconverter-install.log`)

---

## 🪟 步骤 3: 打包 Windows EXE (基础版本)

> **⚠️ 重要说明**: Windows 打包目前是基础版本,无法像 macOS PKG 那样提供完整的自动化安装体验。

### 当前 Windows 打包功能

```batch
REM 在 Windows 上执行
scripts\build-windows.bat
```

**输出:**
```
=== 开始构建 Windows 应用 ===
==> 编译 Windows 可执行文件...
    ✅ 可执行文件已生成
==> 创建安装器...
    ✅ 安装器已创建

=== 构建完成 ===
📦 可执行文件: build\windows\ffmpeg-binary.exe
📂 安装器: build\windows\installer\
```

### ✅ Windows 版本已实现

- ✅ Windows 可执行文件编译
- ✅ 基本的批处理安装脚本
- ✅ 服务可以正常运行 (手动启动后)

### ❌ Windows 版本缺失功能 (相比 macOS PKG)

- ❌ **无图形化安装界面** - 只有命令行批处理脚本
- ❌ **无自动安装 FFmpeg** - 用户需要手动下载 FFmpeg
- ❌ **自启动未完全实现** - 只提示用户手动创建快捷方式
- ❌ **无自动启动服务** - 安装后需要手动运行
- ❌ **无卸载程序** - 需要手动删除文件和清理注册表
- ❌ **无安装程序图标**
- ❌ **无"添加/删除程序"集成**

### 用户安装体验 (当前)

1. 用户获得 `installer` 文件夹
2. **手动下载 FFmpeg**:
   - 访问 https://www.gyan.dev/ffmpeg/builds/
   - 下载 FFmpeg 静态二进制
   - 放置到合适位置
3. 右键点击 `install.bat` → "以管理员身份运行"
4. 批处理脚本复制文件到 `C:\Program Files\GoalfyMediaConverter\`
5. **需要手动**:
   - 创建开机自启动快捷方式
   - 首次运行服务

### 🔧 改进 Windows 打包的建议

要达到 macOS PKG 的自动化体验,需要:

1. **使用专业安装程序**:
   - [Inno Setup](https://jrsoftware.org/isinfo.php) (推荐,免费开源)
   - [NSIS](https://nsis.sourceforge.io/)
   - [WiX Toolset](https://wixtoolset.org/)

2. **自动下载 FFmpeg**:
   ```bash
   # 在安装脚本中自动下载
   curl -L -o ffmpeg.zip https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip
   unzip ffmpeg.zip
   ```

3. **注册表自启动**:
   ```batch
   # 添加注册表键值
   reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" ^
     /v "GoalfyMediaConverter" ^
     /t REG_SZ ^
     /d "C:\Program Files\GoalfyMediaConverter\ffmpeg-binary.exe" ^
     /f
   ```

4. **创建 Windows 服务**:
   ```go
   // 使用 golang.org/x/sys/windows/svc
   // 创建真正的 Windows 服务而不是普通程序
   ```

5. **卸载程序**:
   - 添加到"添加/删除程序"
   - 提供卸载脚本清理所有文件

### 📌 当前建议

如果需要 Windows 平台的生产级安装体验:
- ⏸️ **暂时使用基础版本** - 功能可用但需要手动配置
- 🔨 **计划升级** - 使用 Inno Setup 创建图形化安装程序
- 📦 **参考 macOS PKG** - 实现相同的自动化功能

---

## 🐧 步骤 4: 打包 Linux 包

```bash
# 执行打包
./scripts/build-linux.sh
```

**输出:**
```
╔══════════════════════════════════════════════════════════════╗
║           Linux 软件包打包工具                              ║
╚══════════════════════════════════════════════════════════════╝

==> 清理旧文件...
==> 编译 Linux 二进制文件...
    架构: amd64
    ✅ 二进制文件已生成
==> 创建 DEB 包...
    ✅ DEB 包已创建
==> 创建 RPM 包...
    ✅ RPM 包已创建
==> 创建通用安装脚本...
    ✅ 安装脚本已创建

╔══════════════════════════════════════════════════════════════╗
║                 ✅ 打包完成!                                ║
╚══════════════════════════════════════════════════════════════╝

📦 二进制文件: dist/linux/ffmpeg-binary
📦 DEB 包: dist/linux/ffmpeg-binary_1.0.0_amd64.deb
📦 RPM 包: dist/linux/*.rpm
📜 通用安装脚本: dist/linux/install.sh

使用方法:
  Debian/Ubuntu: sudo dpkg -i ffmpeg-binary_1.0.0_amd64.deb
  CentOS/RHEL:   sudo rpm -i ffmpeg-binary-1.0.0-1.*.rpm
  通用方法:      sudo ./install.sh
```

**用户安装体验 (Debian):**

```bash
$ sudo dpkg -i ffmpeg-binary_1.0.0_amd64.deb

正在配置 FFmpeg Binary 服务...
FFmpeg Binary 服务已安装并启动
服务地址: http://127.0.0.1:28888
```

---

## 📋 完整打包检查清单

### 准备阶段

- [x] 更新版本号(在各脚本中)
- [x] 同步 vendor 依赖: `go mod vendor`
- [x] 测试代码编译: `go build`
- [x] 提交所有代码更改

> **重要**: 打包前必须先运行 `go mod vendor` 同步依赖!

### 图标生成

```bash
# 生成所有平台图标
./scripts/generate-icons.sh
```

### macOS 打包

```bash
# 1. 同步 vendor 依赖 (重要!)
go mod vendor

# 2. 打包 PKG
./scripts/build-macos-pkg.sh

# 3. 验证
open dist/macos/GoalfyMediaConverter-Installer.pkg
```

### Windows 打包

```batch
REM 打包
scripts\build-windows.bat

REM 测试(右键以管理员运行)
dist\windows\installer\install.bat
```

### Linux 打包

```bash
# 打包
./scripts/build-linux.sh

# 测试 DEB
sudo dpkg -i dist/linux/ffmpeg-binary_1.0.0_amd64.deb

# 验证服务
curl http://127.0.0.1:28888/health
```

---

## 🎉 发布

### 1. 创建 GitHub Release

```bash
# 创建标签
git tag v1.0.0
git push origin v1.0.0
```

### 2. 上传安装包

在 GitHub Releases 页面上传:

- `GoalfyMediaConverter-Installer.pkg` (macOS PKG)
- `FFmpeg-Binary-Setup.zip` (Windows installer 文件夹压缩)
- `ffmpeg-binary_1.0.0_amd64.deb` (Linux Debian)
- `ffmpeg-binary-1.0.0-1.rpm` (Linux RPM)

### 3. 发布说明示例

```markdown
## GoalfyMediaConverter v1.0.0

### 功能特性

- ✅ WebM 到 MP4 视频转换
- ✅ 视频切割功能 (根据时间区间删除片段)
- ✅ 分片上传支持 (大文件友好)
- ✅ 实时转换进度查询
- ✅ 批量文件管理
- ✅ 自动端口配置 (28888)
- ✅ 开机自启动
- ✅ 本地服务 (127.0.0.1)
- ✅ 自动安装 FFmpeg (无需 Homebrew)

### 下载

- [macOS (PKG)](GoalfyMediaConverter-Installer.pkg) - 支持 Intel 和 Apple Silicon
- [Windows (ZIP)](FFmpeg-Binary-Setup.zip) - Windows 10/11
- [Linux DEB](ffmpeg-binary_1.0.0_amd64.deb) - Ubuntu/Debian
- [Linux RPM](ffmpeg-binary-1.0.0-1.rpm) - CentOS/RHEL/Fedora

### 安装说明

详见项目文档

### 系统要求

- macOS 10.15+ / Windows 10+ / Linux (现代发行版)
- 无需预装 FFmpeg (macOS 自动安装)
```

---

## 💡 技巧和提示

### macOS 签名(可选)

如果有开发者证书,可以签名应用:

```bash
# 签名
codesign --deep --force --verify --verbose \
  --sign "Developer ID Application: Your Name" \
  dist/macos/FFmpeg-Binary.app

# 验证
codesign --verify --deep --strict --verbose=2 \
  dist/macos/FFmpeg-Binary.app
```

### Windows 图标嵌入

如果想嵌入图标到 EXE:

```bash
# 安装工具
go install github.com/tc-hib/go-winres@latest

# 生成资源文件
go-winres simply --icon assets/icons/icon.ico

# 重新编译
go build -ldflags="-s -w -H windowsgui" -o dist/windows/FFmpeg-Binary.exe
```

### 自动化构建

创建 `Makefile`:

```makefile
.PHONY: icons macos windows linux all

icons:
	./scripts/generate-icons.sh

macos: icons
	./scripts/build-macos-pkg.sh

windows: icons
	./scripts/build-windows.bat

linux: icons
	./scripts/build-linux.sh

all: icons macos linux
	@echo "✅ 所有平台打包完成!"
```

使用:
```bash
make all
```

---

**完成!** 🎉

现在你有了:
- 美观的应用图标
- 专业的 macOS PKG 安装包
- 基础的 Windows 安装器
- 标准的 Linux 软件包
- 完整的文档

用户可以轻松安装并使用你的服务!
